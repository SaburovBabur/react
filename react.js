const button = <button>Salom Dunyo</button>

/**
 * ========================
 * React app Initialization
 */
const app = document.querySelector('#app')
const root = ReactDOM.createRoot(app)
root.render(button)
